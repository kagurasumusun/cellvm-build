#pragma once
/* MSVC-style case-insensitive #include spelling.  The real header is
   windows.h in this same include directory; the Linux build host's
   lookup is case-sensitive, so the alias is spelled out here.
   (Generated-file shape: keep in sync with
   sysroot/gen-include-aliases.py, which emits the same thing for any
   further mismatched spellings an app tree turns up.)  */
#include "windows.h"
